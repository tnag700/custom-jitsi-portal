#!/bin/sh
set -eu

export VAULT_ADDR="${VAULT_ADDR:-http://127.0.0.1:8200}"
ROLE_NAME="${ROLE_NAME:-backup-runner}"

echo "Issue a one-time wrapped secret_id for the backup runner and deliver it out-of-band."
vault write -wrap-ttl=5m -f "auth/approle-workloads/role/${ROLE_NAME}/secret-id"