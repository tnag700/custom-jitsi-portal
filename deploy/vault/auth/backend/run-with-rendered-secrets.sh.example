#!/bin/sh
set -eu

SECRETS_FILE="${BACKEND_SECRETS_FILE:-/vault/runtime/backend/runtime.env}"

if [ ! -r "$SECRETS_FILE" ]; then
  echo "backend secrets file is missing: $SECRETS_FILE" >&2
  exit 1
fi

set -a
. "$SECRETS_FILE"
set +a

exec "$@"